syntax = 'proto3';

package request;

message Packet {
    uint32 handlerId = 1;
    string userId = 2;
    string version = 3;
    uint32 sequence = 4;
    bytes payload = 5;
}

message Ping {
    int64 timestamp = 1;
}